from enum import Enum

class VehicleModelTypes(Enum):
    HATCH = "hatch"
    SEDAN = "sedan"
    CONVERTIBLE = "convertible"

class Colors(Enum):
    BLUE = "blue"
    YELLOW = "yellow"
    GRAY = "gray"
